const os = require("os");

console.log(os.cpus()); //quantos cpus existem
console.log(os.freemem()); //quanto de memoria existe na máquina;
console.log(os.homedir()); //qual o diretório da home (principal)
console.log(os.type()); //qual sistema operacional está rodando na máquina.


